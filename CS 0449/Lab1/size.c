#include <stdio.h>
#include "size.h"

int main() {
    printf("The size of struct A is %lu\n", sizeof(A));
    printf("The size of struct B is %lu\n", sizeof(B));
    printf("The size of struct C is %lu\n", sizeof(C));

    return 0;
}